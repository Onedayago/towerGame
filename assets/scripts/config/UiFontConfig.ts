/**
 * UI 字体尺寸配置
 * 统一管理所有字体相关的尺寸常量
 */
export class UiFontConfig {
    /** 默认字体大小 */
    static readonly DEFAULT_FONT_SIZE = 20;

    /** 小字体大小（按钮标签等） */
    static readonly SMALL_FONT_SIZE = 16;

    /** 中等字体大小（引导文字等） */
    static readonly MEDIUM_FONT_SIZE = 24;

    /** 大字体大小（副标题等） */
    static readonly LARGE_FONT_SIZE = 48;

    /** 引导按钮字体大小 */
    static readonly GUIDE_BUTTON_FONT_SIZE = 18;
}

