# 游戏设计文档

## 目录
1. [游戏概述](#游戏概述)
2. [核心玩法](#核心玩法)
3. [系统架构](#系统架构)
4. [武器系统](#武器系统)
5. [敌人系统](#敌人系统)
6. [UI系统](#ui系统)
7. [配置系统](#配置系统)
8. [扩展指南](#扩展指南)

---

## 游戏概述

### 游戏类型
塔防游戏（Tower Defense）

### 游戏平台
微信小游戏

### 技术栈
- Canvas 2D 渲染
- ES6 模块化
- 微信小游戏 API

### 核心机制
- 玩家在网格地图上放置防御塔（武器）
- 敌人沿固定路径移动，试图到达终点
- 武器自动攻击范围内的敌人
- 击败敌人获得金币，用于购买和升级武器
- 多波次敌人，难度递增

---

## 核心玩法

### 游戏流程
1. **开始阶段**：玩家查看开始界面，选择开始游戏或查看帮助
2. **准备阶段**：玩家使用金币购买武器，放置在战场网格上
3. **战斗阶段**：敌人从起点生成，沿路径移动，武器自动攻击
4. **升级阶段**：击败敌人获得金币，可以升级或出售武器
5. **波次管理**：每波敌人被击败后，进入下一波，难度递增
6. **结束条件**：敌人到达终点（游戏失败）或完成所有波次（游戏胜利）

### 资源系统
- **金币**：主要资源
  - 初始金币：1000
  - 获得方式：击败敌人
  - 消耗方式：购买武器、升级武器
  - 回收方式：出售武器

### 胜利/失败条件
- **胜利**：完成所有波次
- **失败**：敌人到达终点（扣除生命值，生命值归零）

---

## 系统架构

### 目录结构
```
js/
├── config/          # 配置文件
├── core/            # 核心系统
├── entities/        # 游戏实体
├── managers/        # 管理器
├── pathfinding/     # 寻路算法
├── projectiles/     # 抛射物
├── rendering/       # 渲染器
├── ui/              # UI组件
└── utils/           # 工具类
```

### 核心系统

#### 1. GameMain（游戏主控制器）
- **职责**：游戏生命周期管理
- **位置**：`js/GameMain.js`
- **主要功能**：
  - 初始化游戏系统
  - 管理游戏循环
  - 协调各个管理器
  - 处理游戏状态切换

#### 2. GameContext（游戏上下文）
- **职责**：全局状态管理
- **位置**：`js/core/GameContext.js`
- **存储内容**：
  - 游戏状态（开始/暂停/结束）
  - 当前波次
  - 金币数量
  - 管理器引用
  - 战场偏移（拖拽）

#### 3. GameLoop（游戏循环）
- **职责**：游戏主循环
- **位置**：`js/core/GameLoop.js`
- **功能**：
  - 使用 `requestAnimationFrame` 实现循环
  - 计算 deltaTime
  - 调用更新和渲染

#### 4. GameRenderer（游戏渲染器）
- **职责**：统一渲染管理
- **位置**：`js/core/GameRenderer.js`
- **渲染顺序**：
  1. 背景
  2. 障碍物
  3. 武器
  4. 敌人
  5. 抛射物
  6. 粒子效果
  7. UI元素

### 管理器系统

#### WeaponManager（武器管理器）
- **职责**：管理所有武器
- **位置**：`js/managers/WeaponManager.js`
- **功能**：
  - 武器创建和销毁
  - 武器放置验证
  - 武器更新和渲染
  - 武器选择管理

#### EnemyManager（敌人管理器）
- **职责**：管理所有敌人
- **位置**：`js/managers/EnemyManager.js`
- **功能**：
  - 敌人生成
  - 敌人更新和渲染
  - 波次管理
  - 敌人路径管理

#### GoldManager（金币管理器）
- **职责**：管理金币系统
- **位置**：`js/managers/GoldManager.js`
- **功能**：
  - 金币增减
  - 金币显示
  - 购买验证

#### ObstacleManager（障碍物管理器）
- **职责**：管理战场障碍物
- **位置**：`js/managers/ObstacleManager.js`
- **功能**：
  - 障碍物生成
  - 碰撞检测
  - 路径阻挡

---

## 武器系统

### 武器基类（Weapon）
- **位置**：`js/entities/Weapon.js`
- **属性**：
  - `weaponType`: 武器类型
  - `level`: 等级（1-3）
  - `hp`: 生命值
  - `fireInterval`: 开火间隔
  - `attackRange`: 攻击范围
  - `damage`: 伤害值
  - `currentTarget`: 当前目标

### 武器类型

#### 1. 火箭塔（RocketTower）
- **类型**：`WeaponType.ROCKET`
- **特点**：追踪火箭，高爆溅射伤害
- **抛射物**：`HomingRocket`
- **配置**：`js/config/weapons/RocketTowerConfig.js`
- **渲染器**：`js/rendering/RocketTowerRenderer.js`

#### 2. 激光塔（LaserTower）
- **类型**：`WeaponType.LASER`
- **特点**：持续射线，高射速攻击
- **抛射物**：`LaserBeam`
- **配置**：`js/config/weapons/LaserTowerConfig.js`
- **渲染器**：`js/rendering/LaserTowerRenderer.js`

#### 3. 加农炮（CannonTower）
- **类型**：`WeaponType.CANNON`
- **特点**：直线炮弹，高爆伤害
- **抛射物**：`CannonBullet`
- **配置**：`js/config/weapons/CannonTowerConfig.js`
- **渲染器**：`js/rendering/CannonTowerRenderer.js`

#### 4. 狙击塔（SniperTower）
- **类型**：`WeaponType.SNIPER`
- **特点**：快速子弹，超远射程
- **抛射物**：`SniperBullet`
- **配置**：`js/config/weapons/SniperTowerConfig.js`
- **渲染器**：`js/rendering/SniperTowerRenderer.js`

### 武器机制

#### 目标选择
- 武器自动搜索攻击范围内的敌人
- 优先选择距离最近的敌人
- 目标丢失后重新搜索

#### 攻击系统
- 武器旋转指向目标
- 根据 `fireInterval` 控制开火频率
- 生成对应类型的抛射物

#### 升级系统
- 武器可升级3次（1-3级）
- 升级提升伤害和射速
- 升级消耗金币
- 满级后无法继续升级

#### 出售系统
- 可以出售已放置的武器
- 获得部分金币返还
- 出售后武器立即移除

### 添加新武器

#### 步骤1：创建武器配置
在 `js/config/weapons/` 创建配置文件，例如 `NewWeaponConfig.js`：
```javascript
export class NewWeaponConfig {
  static BASE_COST = 100;
  static UPGRADE_COST = 50;
  static SELL_GAIN = 50;
  static FIRE_INTERVAL = 1000;
  static ATTACK_RANGE = 5;
  static DAMAGE_MULTIPLIER = 2;
  // 等级属性
  static LEVEL_1_FIRE_INTERVAL_MULTIPLIER = 1.2;
  static LEVEL_1_DAMAGE_MULTIPLIER = 2;
  // ...
}
```

#### 步骤2：创建抛射物配置
在 `js/config/weapons/` 创建抛射物配置，例如 `NewWeaponBulletConfig.js`：
```javascript
export class NewWeaponBulletConfig {
  static SPEED_MULTIPLIER = 1.0;
  static RADIUS = 8;
  static MAX_LIFETIME = 2000;
}
```

#### 步骤3：注册武器类型
在 `js/config/WeaponConfig.js` 中添加：
```javascript
export const WeaponType = {
  // ... 现有类型
  NEW_WEAPON: 'newWeapon'
};

// 在 WeaponConfigs.CONFIGS 中添加配置
[WeaponType.NEW_WEAPON, {
  id: 'newWeapon',
  name: '新武器',
  icon: '🔫',
  description: '武器描述',
  baseCost: NewWeaponConfig.BASE_COST,
  upgradeCost: NewWeaponConfig.UPGRADE_COST,
  sellGain: NewWeaponConfig.SELL_GAIN,
  colorHex: 0x00ff00,
}]
```

#### 步骤4：创建武器实体
在 `js/entities/` 创建 `NewWeapon.js`：
```javascript
import { Weapon } from './Weapon';
import { WeaponType } from '../config/WeaponConfig';
import { NewWeaponConfig } from '../config/weapons/NewWeaponConfig';
import { NewWeaponBullet } from '../projectiles/NewWeaponBullet';

export class NewWeapon extends Weapon {
  constructor(ctx, x, y) {
    super(ctx, x, y, WeaponType.NEW_WEAPON);
    // 初始化属性
    this.fireInterval = NewWeaponConfig.FIRE_INTERVAL;
    this.attackRange = NewWeaponConfig.ATTACK_RANGE;
    this.damage = WeaponStatsConfig.BULLET_BASE_DAMAGE * NewWeaponConfig.DAMAGE_MULTIPLIER;
    this.bullets = [];
    this.applyLevelStats();
  }
  
  // 实现 fire、update、render 等方法
}
```

#### 步骤5：创建抛射物
在 `js/projectiles/` 创建 `NewWeaponBullet.js`：
```javascript
import { NewWeaponBulletConfig } from '../config/weapons/NewWeaponBulletConfig';

export class NewWeaponBullet {
  constructor(ctx, x, y, targetX, targetY, damage) {
    // 实现抛射物逻辑
  }
}
```

#### 步骤6：创建渲染器
在 `js/rendering/` 创建 `NewWeaponRenderer.js`：
```javascript
export class NewWeaponRenderer {
  static render(ctx, x, y, size, level = 1, angle = 0) {
    // 实现渲染逻辑
  }
}
```

#### 步骤7：更新 WeaponRenderer
在 `js/rendering/WeaponRenderer.js` 中添加：
```javascript
import { NewWeaponRenderer } from './NewWeaponRenderer';

// 在 renderWeaponIcon 中添加
if (weaponType === WeaponType.NEW_WEAPON) {
  NewWeaponRenderer.render(ctx, x, y, size, 1);
}
```

#### 步骤8：更新 WeaponManager
在 `js/managers/WeaponManager.js` 中添加：
```javascript
import { NewWeapon } from '../entities/NewWeapon';

// 在 placeWeapon 中添加
if (weaponType === WeaponType.NEW_WEAPON) {
  weapon = new NewWeapon(this.ctx, gridX, gridY);
}
```

#### 步骤9：更新颜色配置
在 `js/config/Colors.js` 中添加武器颜色：
```javascript
static NEW_WEAPON_BASE = 0x00ff00;
static NEW_WEAPON_TOWER = 0x00ff00;
static NEW_WEAPON_DETAIL = 0x00ff00;
static NEW_WEAPON_BULLET = 0x00ff00;
```

---

## 敌人系统

### 敌人基类（Enemy）
- **位置**：`js/entities/Enemy.js`
- **属性**：
  - `enemyType`: 敌人类型
  - `hp`: 生命值
  - `speed`: 移动速度
  - `damage`: 攻击伤害
  - `goldReward`: 金币奖励
  - `pathIndex`: 路径索引

### 敌人类型

#### 1. 坦克敌人（EnemyTank）
- **特点**：基础敌人，平衡属性
- **配置**：`js/config/enemies/EnemyTankConfig.js`
- **渲染器**：`js/rendering/enemies/EnemyTankRenderer.js`

#### 2. 快速敌人（FastEnemy）
- **特点**：高速度，低生命值
- **配置**：`js/config/enemies/FastEnemyConfig.js`
- **渲染器**：`js/rendering/enemies/FastEnemyRenderer.js`

#### 3. 重装敌人（HeavyEnemy）
- **特点**：高生命值，低速度
- **配置**：`js/config/enemies/HeavyEnemyConfig.js`
- **渲染器**：`js/rendering/enemies/HeavyEnemyRenderer.js`

#### 4. 飞行敌人（FlyingEnemy）
- **特点**：可以飞越障碍物
- **配置**：`js/config/enemies/FlyingEnemyConfig.js`
- **渲染器**：`js/rendering/enemies/FlyingEnemyRenderer.js`

#### 5. 轰炸敌人（BomberEnemy）
- **特点**：死亡时爆炸，造成范围伤害
- **配置**：`js/config/enemies/BomberEnemyConfig.js`
- **渲染器**：`js/rendering/enemies/BomberEnemyRenderer.js`

### 敌人机制

#### 移动系统
- 使用 A* 寻路算法
- 网格化移动（只允许水平/垂直）
- 碰撞检测和分离
- 路径跟随

#### 攻击系统
- 敌人可以攻击武器
- 到达终点扣除玩家生命值

#### 生成系统
- 波次管理
- 按配置生成敌人
- 生成间隔控制

### 添加新敌人

#### 步骤1：创建敌人配置
在 `js/config/enemies/` 创建配置文件：
```javascript
export class NewEnemyConfig {
  static HP = 100;
  static SPEED = 1.0;
  static DAMAGE = 10;
  static GOLD_REWARD = 20;
  static RADIUS = 15;
}
```

#### 步骤2：注册敌人类型
在 `js/config/EnemyConfig.js` 中添加类型和配置

#### 步骤3：创建敌人实体
在 `js/entities/` 创建 `NewEnemy.js`，继承 `Enemy`

#### 步骤4：创建渲染器
在 `js/rendering/enemies/` 创建 `NewEnemyRenderer.js`

#### 步骤5：更新 EnemyManager
在 `js/managers/EnemyManager.js` 中添加生成逻辑

---

## UI系统

### UI组件

#### 1. StartScreen（开始界面）
- **位置**：`js/ui/StartScreen.js`
- **功能**：游戏开始界面，显示标题和按钮

#### 2. HelpScreen（帮助界面）
- **位置**：`js/ui/HelpScreen.js`
- **功能**：游戏说明和帮助信息

#### 3. LoadingScreen（加载界面）
- **位置**：`js/ui/LoadingScreen.js`
- **功能**：游戏加载进度显示

#### 4. WeaponContainerUI（武器容器）
- **位置**：`js/ui/WeaponContainerUI.js`
- **功能**：
  - 显示武器卡片
  - 武器拖拽放置
  - 滚动查看武器

#### 5. BattlefieldMinimap（小地图）
- **位置**：`js/ui/BattlefieldMinimap.js`
- **功能**：
  - 显示战场缩略图
  - 显示当前视野
  - 点击/拖拽移动视野

#### 6. GoldManager（金币显示）
- **位置**：`js/managers/GoldManager.js`
- **功能**：显示和管理金币

#### 7. WaveInfo（波次信息）
- **位置**：`js/ui/components/WaveInfo.js`
- **功能**：显示当前波次信息

### UI优化
- 使用离屏Canvas缓存静态部分
- 武器卡片使用缓存渲染
- 背景和箭头使用缓存渲染

---

## 配置系统

### 配置文件结构

#### GameConfig（游戏配置）
- **位置**：`js/config/GameConfig.js`
- **内容**：
  - 设计尺寸
  - 网格系统
  - 战场尺寸
  - 单元格大小

#### UIConfig（UI配置）
- **位置**：`js/config/UIConfig.js`
- **内容**：
  - UI尺寸
  - 按钮尺寸
  - 字体大小
  - 间距和边距

#### Colors（颜色配置）
- **位置**：`js/config/Colors.js`
- **内容**：
  - 武器颜色
  - 敌人颜色
  - UI颜色
  - 背景颜色

#### ParticleConfig（粒子配置）
- **位置**：`js/config/ParticleConfig.js`
- **内容**：
  - 粒子数量
  - 粒子生命周期
  - 粒子速度

#### WaveConfig（波次配置）
- **位置**：`js/config/WaveConfig.js`
- **内容**：
  - 波次数量
  - 敌人生成配置
  - 难度曲线

### 配置原则
- 所有数值配置化
- 使用静态类存储配置
- 支持动态计算（getter）
- 集中管理，便于调整

---

## 扩展指南

### 添加新功能的一般流程

1. **设计阶段**
   - 确定功能需求
   - 设计数据结构
   - 规划系统交互

2. **实现阶段**
   - 创建配置文件
   - 实现核心逻辑
   - 实现渲染器
   - 集成到管理器

3. **测试阶段**
   - 功能测试
   - 性能测试
   - UI测试

4. **优化阶段**
   - 性能优化
   - 代码重构
   - 添加缓存

### 性能优化建议

#### 渲染优化
- 使用离屏Canvas缓存静态内容
- 实现视锥剔除（Frustum Culling）
- 批量渲染相同类型对象
- 减少Canvas状态切换

#### 更新优化
- 使用对象池管理临时对象
- 避免频繁创建/销毁对象
- 优化碰撞检测算法
- 使用空间分割（如四叉树）

#### 内存优化
- 及时清理不需要的对象
- 复用Canvas和上下文
- 避免内存泄漏
- 控制粒子数量

### 代码规范

#### 命名规范
- 类名：PascalCase（如 `WeaponManager`）
- 方法名：camelCase（如 `updateWeapon`）
- 常量：UPPER_SNAKE_CASE（如 `MAX_LEVEL`）
- 私有属性：下划线前缀（如 `_cachedCanvas`）

#### 文件组织
- 一个类一个文件
- 相关功能放在同一目录
- 配置文件集中管理
- 工具类独立目录

#### 注释规范
- 类和方法使用 JSDoc 注释
- 复杂逻辑添加行内注释
- 配置项说明用途
- 重要算法说明原理

### 常见扩展场景

#### 场景1：添加新武器类型
参考 [武器系统 - 添加新武器](#添加新武器)

#### 场景2：添加新敌人类型
参考 [敌人系统 - 添加新敌人](#添加新敌人)

#### 场景3：添加新UI组件
1. 在 `js/ui/` 创建组件文件
2. 实现 `render()` 方法
3. 在 `GameMain` 中初始化
4. 在 `GameRenderer` 中渲染

#### 场景4：添加新特效
1. 在 `ParticleManager` 中添加特效类型
2. 在 `ParticleConfig` 中添加配置
3. 在需要的地方调用创建方法

#### 场景5：添加新游戏模式
1. 在 `GameContext` 中添加模式状态
2. 在 `GameMain` 中实现模式逻辑
3. 在 `GameRenderer` 中实现模式渲染
4. 在UI中添加模式切换

---

## 版本历史

### v1.0.0（当前版本）
- ✅ 基础塔防玩法
- ✅ 4种武器类型
- ✅ 5种敌人类型
- ✅ 完整的UI系统
- ✅ 波次系统
- ✅ 升级和出售系统
- ✅ 离屏渲染优化

---

## 待实现功能

### 短期计划
- [ ] 音效系统
- [ ] 更多武器类型
- [ ] 更多敌人类型
- [ ] 技能系统
- [ ] 成就系统

### 长期计划
- [ ] 多关卡系统
- [ ] 难度选择
- [ ] 排行榜
- [ ] 社交功能
- [ ] 数据统计

---

## 联系方式

如有问题或建议，请参考项目 README 或提交 Issue。

